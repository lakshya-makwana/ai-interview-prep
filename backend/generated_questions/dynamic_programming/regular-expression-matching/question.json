{
    "title": "Bio-Pattern Sequence Validator",
    "slug": "regular-expression-matching",
    "difficulty": "hard",
    "category": "dynamic_programming",
    "description": "You are developing a secure genomics pipeline that needs to verify if an unknown DNA sample sequence matches a specific rule pattern. The pattern can contain lowercase English letters, '.', which matches any single character, and '*', which indicates that the preceding element can appear zero or more times. The matching must cover the entire input sequence, not just a substring.",
    "constraints": [
        "1 <= text.length <= 20",
        "1 <= pattern.length <= 20",
        "text contains only lowercase English letters.",
        "pattern contains only lowercase English letters, '.', and '*'.",
        "It is guaranteed for each appearance of '*', there is a previous valid character to match."
    ],
    "input_format": "Two strings: text (the DNA sequence) and pattern (the rule pattern).",
    "output_format": "A boolean value indicating whether the pattern matches the entire text.",
    "estimated_time": 45,
    "acceptance_rate": 34.0,
    "is_premium": false,
    "examples": [
        {
            "input": "text = \"aa\", pattern = \"a\"",
            "output": "false",
            "explanation": "'a' does not match the entire string \"aa\"."
        },
        {
            "input": "text = \"aa\", pattern = \"a*\"",
            "output": "true",
            "explanation": "'*' means zero or more of the preceding element, 'a'. Therefore, repeating 'a' twice matches \"aa\"."
        },
        {
            "input": "text = \"ab\", pattern = \".*\"",
            "output": "true",
            "explanation": "\".*\" means \"zero or more (*) of any character (.)\", which matches any string."
        }
    ]
}